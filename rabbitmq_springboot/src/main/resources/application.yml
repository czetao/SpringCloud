spring:
  application:
    name: rabbitmq
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin
    virtual-host: /
    # 提前抓取消息的个数，处理完一条再拿一条
    listener:
      simple:
        prefetch: 1
